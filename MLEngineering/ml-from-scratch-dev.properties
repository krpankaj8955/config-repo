# Dev profile: local Postgres with sensible defaults (override via env if needed).
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/mlengineering}
spring.datasource.username=${DATABASE_USERNAME:mluser}
spring.datasource.password=${DATABASE_PASSWORD:devpassword}
