# Dev profile: PostgreSQL (datasource inherited from lottery-backend.properties)
# with demo data seeded on first run.

# Seed demo data on first startup only (DataLoader skips if data already exists).
app.seed-demo-data=true

# --- Dev auth defaults (so no env vars are needed in dev) ---
# Google Web client id (public value). Must match lottery-frontend VITE_GOOGLE_CLIENT_ID.
app.google.client-id=${GOOGLE_CLIENT_ID:415000850783-8drrqdcqfhism67rg6abkdc61v1921n3.apps.googleusercontent.com}
# Throwaway admin credentials for LOCAL development only (never the prod secret).
app.admin.email=${ADMIN_EMAIL:lottery-admin@gmail.com}
app.admin.password=${ADMIN_PASSWORD:admin123}
